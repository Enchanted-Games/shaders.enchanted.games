# Vanilla Shader Docs
Unoffical documentation for Minecraft Java Edition resource pack shaders

## Dev setup

1. Download Node.js and pnpm if you haven't already
2. Run `pnpn install` at the project root to install the required packages
3. Run `pnpn docs:dev` to start a live dev server
